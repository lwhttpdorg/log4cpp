diff --git a/.clang-format b/.clang-format
index 75815e5..4d3b6a2 100644
--- a/.clang-format
+++ b/.clang-format
@@ -6,11 +6,11 @@ AlignConsecutiveAssignments: false
 AlignConsecutiveDeclarations: false
 AlignOperands: Align
 AlignTrailingComments: true
-AllowShortBlocksOnASingleLine: false
+AllowShortBlocksOnASingleLine: Empty
 AllowShortFunctionsOnASingleLine: None
 AllowShortIfStatementsOnASingleLine: true
 AllowShortLoopsOnASingleLine: true
-AlwaysBreakAfterDefinitionReturnType: false
+AlwaysBreakAfterDefinitionReturnType: None
 AlwaysBreakTemplateDeclarations: Yes
 AlignEscapedNewlines: DontAlign
 BraceWrapping: 
@@ -63,4 +63,5 @@ SpacesInCStyleCastParentheses: false
 SpacesInParentheses: false
 TabWidth: 4
 UseTab: Never
-...
+Cpp11BracedListStyle: true
+InsertBraces: true
diff --git a/build-deb.sh b/build-deb.sh
index b2fe5ac..93a5ff8 100755
--- a/build-deb.sh
+++ b/build-deb.sh
@@ -1,18 +1,85 @@
 #!/bin/bash
 set -e
 
-if [[ "$1" == "clean" ]]; then
-    echo "==> Running fakeroot debian/rules clean..."
-    fakeroot debian/rules clean
+usage() {
+    echo "Usage: $0 [clean] [--arch <ARCH>]"
+    echo
+    echo "Options:"
+    echo "  clean          Clean previous build artifacts"
+    echo "  --arch <ARCH>  Cross-compile for the specified architecture"
+    echo "                 Supported: aarch64, armhf, mips, mips64"
+    echo
+    echo "Examples:"
+    echo "  $0                    # native build"
+    echo "  $0 --arch aarch64    # cross-compile for aarch64"
+    echo "  $0 clean             # clean build artifacts"
+    exit 0
+}
 
-    echo "==> Removing generated .deb files..."
-    find .. -maxdepth 1 -type f -name "*.deb" -delete
+# Map short arch names to dpkg architecture and cmake toolchain file
+resolve_arch() {
+    local arch="$1"
+    case "$arch" in
+        aarch64|arm64)
+            DEB_HOST_ARCH="arm64"
+            CMAKE_TOOLCHAIN="cross/aarch64-linux-gnu.cmake"
+            CROSS_COMPILER_PREFIX="aarch64-linux-gnu"
+            ;;
+        armhf|arm)
+            DEB_HOST_ARCH="armhf"
+            CMAKE_TOOLCHAIN="cross/arm-linux-gnueabihf.cmake"
+            CROSS_COMPILER_PREFIX="arm-linux-gnueabihf"
+            ;;
+        mips)
+            DEB_HOST_ARCH="mips"
+            CMAKE_TOOLCHAIN="cross/mips-linux-gnu.cmake"
+            CROSS_COMPILER_PREFIX="mips-linux-gnu"
+            ;;
+        mips64)
+            DEB_HOST_ARCH="mips64"
+            CMAKE_TOOLCHAIN="cross/mips64-linux-gnuabi64.cmake"
+            CROSS_COMPILER_PREFIX="mips64-linux-gnuabi64"
+            ;;
+        *)
+            echo "Error: unsupported architecture '$arch'"
+            echo "Supported: aarch64, armhf, mips, mips64"
+            exit 1
+            ;;
+    esac
+}
 
-    echo "==> Clean completed."
-    exit 0
-fi
+CROSS_BUILD=0
+TARGET_ARCH=""
+
+# Parse arguments
+while [[ $# -gt 0 ]]; do
+    case "$1" in
+        clean)
+            echo "==> Running fakeroot debian/rules clean..."
+            fakeroot debian/rules clean
+
+            echo "==> Removing generated .deb files..."
+            find .. -maxdepth 1 -type f -name "*.deb" -delete
 
-# Required dependencies
+            echo "==> Clean completed."
+            exit 0
+            ;;
+        --arch|-a)
+            CROSS_BUILD=1
+            TARGET_ARCH="$2"
+            shift 2
+            ;;
+        --help|-h)
+            usage
+            ;;
+        *)
+            echo "Unknown option: $1"
+            usage
+            ;;
+    esac
+done
+
+# Required dependencies (common)
 REQUIRED_PKGS=(
     dpkg-dev
     fakeroot
@@ -20,6 +87,11 @@ REQUIRED_PKGS=(
     nlohmann-json3-dev
 )
 
+if [[ "$CROSS_BUILD" -eq 1 ]]; then
+    resolve_arch "$TARGET_ARCH"
+    REQUIRED_PKGS+=("gcc-${CROSS_COMPILER_PREFIX}" "g++-${CROSS_COMPILER_PREFIX}")
+fi
+
 echo "==> Checking required dependencies..."
 
 MISSING_PKGS=()
@@ -48,7 +120,16 @@ echo "==> Cleaning previous build..."
 fakeroot debian/rules clean
 
 echo "==> Building the .deb package..."
-DEB_BUILD_OPTIONS="noddebs" dpkg-buildpackage -us -uc -b -j"$(nproc)"
+
+if [[ "$CROSS_BUILD" -eq 1 ]]; then
+    echo "==> Cross-compiling for ${DEB_HOST_ARCH} (toolchain: ${CMAKE_TOOLCHAIN})"
+    export CMAKE_TOOLCHAIN_FILE="$(pwd)/${CMAKE_TOOLCHAIN}"
+    export CC="${CROSS_COMPILER_PREFIX}-gcc"
+    export CXX="${CROSS_COMPILER_PREFIX}-g++"
+    DEB_BUILD_OPTIONS="noddebs nocheck" dpkg-buildpackage -us -uc -b -j"$(nproc)" -a"${DEB_HOST_ARCH}"
+else
+    DEB_BUILD_OPTIONS="noddebs" dpkg-buildpackage -us -uc -b -j"$(nproc)"
+fi
 
 echo "==> Removing unnecessary build files..."
 find .. -maxdepth 1 -type f \( -name "*.buildinfo" -o -name "*.changes" \) -delete
diff --git a/build-rpm.sh b/build-rpm.sh
index b05f89c..f74d7f8 100755
--- a/build-rpm.sh
+++ b/build-rpm.sh
@@ -1,12 +1,77 @@
 #!/bin/bash
 set -e
 
-if [[ "$1" == "clean" ]]; then
-    echo "==> Removing entire rpmbuild directory..."
-    rm -rf ~/rpmbuild
-    echo "==> Clean completed."
+usage() {
+    echo "Usage: $0 [clean] [--arch <ARCH>]"
+    echo
+    echo "Options:"
+    echo "  clean          Clean rpmbuild directory"
+    echo "  --arch <ARCH>  Cross-compile for the specified architecture"
+    echo "                 Supported: aarch64, armhf, mips, mips64"
+    echo
+    echo "Examples:"
+    echo "  $0                    # native build"
+    echo "  $0 --arch aarch64    # cross-compile for aarch64"
+    echo "  $0 clean             # clean build artifacts"
     exit 0
-fi
+}
+
+resolve_arch() {
+    local arch="$1"
+    case "$arch" in
+        aarch64|arm64)
+            RPM_TARGET_ARCH="aarch64"
+            CMAKE_TOOLCHAIN="cross/aarch64-linux-gnu.cmake"
+            CROSS_COMPILER_PREFIX="aarch64-linux-gnu"
+            ;;
+        armhf|arm)
+            RPM_TARGET_ARCH="armv7hl"
+            CMAKE_TOOLCHAIN="cross/arm-linux-gnueabihf.cmake"
+            CROSS_COMPILER_PREFIX="arm-linux-gnueabihf"
+            ;;
+        mips)
+            RPM_TARGET_ARCH="mips"
+            CMAKE_TOOLCHAIN="cross/mips-linux-gnu.cmake"
+            CROSS_COMPILER_PREFIX="mips-linux-gnu"
+            ;;
+        mips64)
+            RPM_TARGET_ARCH="mips64"
+            CMAKE_TOOLCHAIN="cross/mips64-linux-gnuabi64.cmake"
+            CROSS_COMPILER_PREFIX="mips64-linux-gnuabi64"
+            ;;
+        *)
+            echo "Error: unsupported architecture '$arch'"
+            echo "Supported: aarch64, armhf, mips, mips64"
+            exit 1
+            ;;
+    esac
+}
+
+CROSS_BUILD=0
+TARGET_ARCH=""
+
+while [[ $# -gt 0 ]]; do
+    case "$1" in
+        clean)
+            echo "==> Removing entire rpmbuild directory..."
+            rm -rf ~/rpmbuild
+            echo "==> Clean completed."
+            exit 0
+            ;;
+        --arch|-a)
+            CROSS_BUILD=1
+            TARGET_ARCH="$2"
+            shift 2
+            ;;
+        --help|-h)
+            usage
+            ;;
+        *)
+            echo "Unknown option: $1"
+            usage
+            ;;
+    esac
+done
 
 # Required dependencies
 REQUIRED_PKGS=(
@@ -17,6 +82,10 @@ REQUIRED_PKGS=(
     json-devel
 )
 
+if [[ "$CROSS_BUILD" -eq 1 ]]; then
+    resolve_arch "$TARGET_ARCH"
+fi
+
 echo "==> Checking required dependencies..."
 
 MISSING_PKGS=()
@@ -55,6 +124,17 @@ echo "==> Writing spec with VERSION from ${SCRIPT_DIR}/VERSION..."
 sed "s/^%define _version .*/%define _version ${VERSION}/" log4cpp/liblog4cpp.spec > "${HOME}/rpmbuild/SPECS/liblog4cpp.spec"
 
 echo "==> Building RPM package..."
-rpmbuild -ba ~/rpmbuild/SPECS/liblog4cpp.spec
+
+if [[ "$CROSS_BUILD" -eq 1 ]]; then
+    echo "==> Cross-compiling for ${RPM_TARGET_ARCH} (toolchain: ${CMAKE_TOOLCHAIN})"
+    export CC="${CROSS_COMPILER_PREFIX}-gcc"
+    export CXX="${CROSS_COMPILER_PREFIX}-g++"
+    rpmbuild -ba ~/rpmbuild/SPECS/liblog4cpp.spec \
+        --target "${RPM_TARGET_ARCH}" \
+        --define "__cmake_in_source_build 1" \
+        --define "_cmake_extra_args -DCMAKE_TOOLCHAIN_FILE=${SCRIPT_DIR}/${CMAKE_TOOLCHAIN}"
+else
+    rpmbuild -ba ~/rpmbuild/SPECS/liblog4cpp.spec
+fi
 
 echo "==> RPM build completed successfully."
diff --git a/code-analysis.sh b/code-analysis.sh
index 4d5169d..d3bb160 100755
--- a/code-analysis.sh
+++ b/code-analysis.sh
@@ -79,5 +79,6 @@ if [ "$errors" -gt 0 ] || [ "$warnings" -gt 0 ] || [ "$notes" -gt 0 ]; then
     echo -e "${YELLOW}Check '${ANALYSIS_REPORT}' for details.${NC}"
 else
     echo -e "✔️ ${GREEN}STATUS: code analysis PASSED.${NC}"
+    rm ${ANALYSIS_REPORT}
 fi
 
diff --git a/code-format.py b/code-format.py
index 855fb4d..e4add6d 100755
--- a/code-format.py
+++ b/code-format.py
@@ -1,4 +1,6 @@
-# encoding=UTF-8
+#!/usr/bin/env python3
+# -*- coding: utf-8 -*-
+
 import os
 
 def fina_all_code_file(base: str):
diff --git a/cross/aarch64-linux-gnu.cmake b/cross/aarch64-linux-gnu.cmake
new file mode 100644
index 0000000..a04f4b2
--- /dev/null
+++ b/cross/aarch64-linux-gnu.cmake
@@ -0,0 +1,25 @@
+set(CMAKE_SYSTEM_NAME Linux)
+set(CMAKE_SYSTEM_PROCESSOR aarch64)
+
+set(CMAKE_C_COMPILER "aarch64-linux-gnu-gcc")
+set(CMAKE_CXX_COMPILER "aarch64-linux-gnu-g++")
+
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_PROGRAM)
+    set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_LIBRARY)
+    set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_INCLUDE)
+    set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_PACKAGE)
+    set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE ONLY)
+endif()
+
+set(CMAKE_C_FLAGS "-march=armv8-a")
+set(CMAKE_CXX_FLAGS "-march=armv8-a")
+
+# cache flags
+set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS}" CACHE STRING "c flags")
+set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}" CACHE STRING "c++ flags")
diff --git a/cross/aarch64-linux-gnu.ini b/cross/aarch64-linux-gnu.ini
new file mode 100644
index 0000000..30e8166
--- /dev/null
+++ b/cross/aarch64-linux-gnu.ini
@@ -0,0 +1,19 @@
+[binaries]
+c = 'aarch64-linux-gnu-gcc'
+cpp = 'aarch64-linux-gnu-g++'
+ar = 'aarch64-linux-gnu-ar'
+strip = 'aarch64-linux-gnu-strip'
+pkgconfig = 'aarch64-linux-gnu-pkg-config'
+
+[host_machine]
+system = 'linux'
+cpu_family = 'aarch64'
+cpu = 'armv8-a'
+endian = 'little'
+
+[properties]
+sys_root = '/usr/aarch64-linux-gnu'
+
+[built-in options]
+c_args = ['-march=armv8-a']
+cpp_args = ['-march=armv8-a']
diff --git a/cross/arm-linux-gnueabihf.cmake b/cross/arm-linux-gnueabihf.cmake
new file mode 100644
index 0000000..9a8cee7
--- /dev/null
+++ b/cross/arm-linux-gnueabihf.cmake
@@ -0,0 +1,25 @@
+set(CMAKE_SYSTEM_NAME Linux)
+set(CMAKE_SYSTEM_PROCESSOR arm)
+
+set(CMAKE_C_COMPILER "arm-linux-gnueabihf-gcc")
+set(CMAKE_CXX_COMPILER "arm-linux-gnueabihf-g++")
+
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_PROGRAM)
+    set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_LIBRARY)
+    set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_INCLUDE)
+    set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_PACKAGE)
+    set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE ONLY)
+endif()
+
+set(CMAKE_C_FLAGS "-march=armv7-a -mfloat-abi=hard -mfpu=neon")
+set(CMAKE_CXX_FLAGS "-march=armv7-a -mfloat-abi=hard -mfpu=neon")
+
+# cache flags
+set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS}" CACHE STRING "c flags")
+set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}" CACHE STRING "c++ flags")
diff --git a/cross/arm-linux-gnueabihf.ini b/cross/arm-linux-gnueabihf.ini
new file mode 100644
index 0000000..c9777f9
--- /dev/null
+++ b/cross/arm-linux-gnueabihf.ini
@@ -0,0 +1,21 @@
+[binaries]
+c = 'arm-linux-gnueabihf-gcc'
+cpp = 'arm-linux-gnueabihf-g++'
+ar = 'arm-linux-gnueabihf-ar'
+strip = 'arm-linux-gnueabihf-strip'
+pkgconfig = 'arm-linux-gnueabihf-pkg-config'
+
+[host_machine]
+system = 'linux'
+cpu_family = 'arm'
+cpu = 'armv7-a'
+endian = 'little'
+
+[properties]
+# sys_root = '/path/to/your/sdk'
+
+[built-in options]
+c_args = ['-march=armv7-a', '-mfloat-abi=hard', '-mfpu=neon']
+cpp_args = ['-march=armv7-a', '-mfloat-abi=hard', '-mfpu=neon']
+c_link_args = ['-march=armv7-a', '-mfloat-abi=hard', '-mfpu=neon']
+cpp_link_args = ['-march=armv7-a', '-mfloat-abi=hard', '-mfpu=neon']
diff --git a/cross/mips-linux-gnu.cmake b/cross/mips-linux-gnu.cmake
new file mode 100644
index 0000000..d60b0f8
--- /dev/null
+++ b/cross/mips-linux-gnu.cmake
@@ -0,0 +1,25 @@
+set(CMAKE_SYSTEM_NAME Linux)
+set(CMAKE_SYSTEM_PROCESSOR mips)
+
+set(CMAKE_C_COMPILER "mips-linux-gnu-gcc")
+set(CMAKE_CXX_COMPILER "mips-linux-gnu-g++")
+
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_PROGRAM)
+    set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_LIBRARY)
+    set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_INCLUDE)
+    set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_PACKAGE)
+    set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE ONLY)
+endif()
+
+set(CMAKE_C_FLAGS "-march=mips32")
+set(CMAKE_CXX_FLAGS "-march=mips32")
+
+# cache flags
+set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS}" CACHE STRING "c flags")
+set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}" CACHE STRING "c++ flags")
diff --git a/cross/mips-linux-gnu.ini b/cross/mips-linux-gnu.ini
new file mode 100644
index 0000000..d50ef93
--- /dev/null
+++ b/cross/mips-linux-gnu.ini
@@ -0,0 +1,15 @@
+[binaries]
+c = 'mips-linux-gnu-gcc'
+cpp = 'mips-linux-gnu-g++'
+ar = 'mips-linux-gnu-ar'
+strip = 'mips-linux-gnu-strip'
+
+[host_machine]
+system = 'linux'
+cpu_family = 'mips'
+cpu = 'mips32'
+endian = 'big'
+
+[built-in options]
+c_args = ['-march=mips32']
+cpp_args = ['-march=mips32']
diff --git a/cross/mips64-linux-gnuabi64.cmake b/cross/mips64-linux-gnuabi64.cmake
new file mode 100644
index 0000000..20ad3c1
--- /dev/null
+++ b/cross/mips64-linux-gnuabi64.cmake
@@ -0,0 +1,25 @@
+set(CMAKE_SYSTEM_NAME Linux)
+set(CMAKE_SYSTEM_PROCESSOR mips64)
+
+set(CMAKE_C_COMPILER "mips64-linux-gnuabi64-gcc")
+set(CMAKE_CXX_COMPILER "mips64-linux-gnuabi64-g++")
+
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_PROGRAM)
+    set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_LIBRARY)
+    set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_INCLUDE)
+    set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
+endif()
+if(NOT CMAKE_FIND_ROOT_PATH_MODE_PACKAGE)
+    set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE ONLY)
+endif()
+
+set(CMAKE_C_FLAGS "-march=mips64")
+set(CMAKE_CXX_FLAGS "-march=mips64")
+
+# cache flags
+set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS}" CACHE STRING "c flags")
+set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}" CACHE STRING "c++ flags")
\ No newline at end of file
diff --git a/cross/mips64-linux-gnuabi64.ini b/cross/mips64-linux-gnuabi64.ini
new file mode 100644
index 0000000..e9019c7
--- /dev/null
+++ b/cross/mips64-linux-gnuabi64.ini
@@ -0,0 +1,15 @@
+[binaries]
+c = 'mips64-linux-gnuabi64-gcc'
+cpp = 'mips64-linux-gnuabi64-g++'
+ar = 'mips64-linux-gnuabi64-ar'
+strip = 'mips64-linux-gnuabi64-strip'
+
+[host_machine]
+system = 'linux'
+cpu_family = 'mips64'
+cpu = 'mips64'
+endian = 'big'
+
+[built-in options]
+c_args = ['-march=mips64']
+cpp_args = ['-march=mips64']
diff --git a/debian/control b/debian/control
index 4ce7e9c..6de616c 100755
--- a/debian/control
+++ b/debian/control
@@ -8,11 +8,13 @@ Homepage: https://github.com/lwhttpdorg/log4cpp
 
 Package: liblog4cpp
 Architecture: any
+Multi-Arch: same
 Depends: ${shlibs:Depends}, ${misc:Depends}
 Description: a log4j-style c++ logging library
 
 Package: liblog4cpp-dev
 Section: libdevel
 Architecture: any
+Multi-Arch: same
 Depends: liblog4cpp (= ${binary:Version}), nlohmann-json3-dev (>= 3.0.0), ${misc:Depends}
 Description: development files for log4cpp
diff --git a/debian/rules b/debian/rules
index 3db32b2..037715c 100755
--- a/debian/rules
+++ b/debian/rules
@@ -2,9 +2,17 @@
 
 export DEB_BUILD_MAINT_OPTIONS = hardening=+all
 
+# Cross-compilation: pick up toolchain file from environment
+ifneq ($(CMAKE_TOOLCHAIN_FILE),)
+  CROSS_CMAKE_FLAGS = -DCMAKE_TOOLCHAIN_FILE=$(CMAKE_TOOLCHAIN_FILE)
+endif
+
 %:
 	dh $@ --buildsystem=cmake
 
+override_dh_auto_configure:
+	dh_auto_configure -- $(CROSS_CMAKE_FLAGS)
+
 override_dh_auto_build:
 	dh_auto_build --parallel VERBOSE=1
 
diff --git a/liblog4cpp.spec b/liblog4cpp.spec
index e004d5d..53cd153 100644
--- a/liblog4cpp.spec
+++ b/liblog4cpp.spec
@@ -35,7 +35,8 @@ developing applications that use %{name}.
 
 %build
 # Standard CMake macro that handles build types and library paths (e.g., /usr/lib64)
-%cmake -DPROJECT_VERSION=%{version}
+# _cmake_extra_args is optionally set by build-rpm.sh for cross-compilation
+%cmake -DPROJECT_VERSION=%{version} %{?_cmake_extra_args}
 %cmake_build
 
 %install

